{
  "postgres": "17",
  "commands": [
    "psql -v ON_ERROR_STOP=1 -f supabase/migrations/0001_initial.sql",
    "psql -v ON_ERROR_STOP=1 -f supabase/migrations/0002_agent_ledger.sql",
    "psql -v ON_ERROR_STOP=1 -f supabase/tests/tenant_isolation.sql",
    "psql -v ON_ERROR_STOP=1 -f supabase/tests/agent_ledger.sql",
    "20 parallel reserve_run_budget calls of 10 tokens against a 100 token budget"
  ],
  "results": {
    "tenantIsolation": "pass",
    "forgedReview": "rejected",
    "forgedPublication": "rejected",
    "crossTenantBudget": "rejected",
    "parallelReservationsPassed": 10,
    "parallelReservationsRejected": 10,
    "reservedTokens": 100,
    "freshBrowserCapability": "pass",
    "fragmentExchangeHttpOnly": "pass",
    "revokedCapability": "rejected",
    "forgedUnrelatedOpportunity": "rejected",
    "unselectedPayloadClaim": "not served"
  },
  "limitations": [
    "Crash-mid-side-effect exactly-once is not yet proven",
    "Capability exchange is proven locally but production identity is not connected",
    "JWT verification belongs to the trusted backend in front of NOLOGIN roles"
  ]
}
